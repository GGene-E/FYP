<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Login</title>
        <link rel="stylesheet" href="../style.css">
    </head>    
    <body>
        
        <% if (show) { %>
        <div class="fixed right-0 h-screen w-1/5 flex items-center justify-end mr-8">
            <% if (color) { %>
            <div class="bg-green-100 p-5 font-VarelaRound border border-green-400">
            <% } else { %>
            <div class="bg-red-100 p-5 font-VarelaRound border border-red-400">
            <% } %>
                <form class="flex" action="/server.js" method="post">
                    <p class="text-lg text-gray-700 font-bold font-VarelaRound"><%= message %></p>
                    <button type="submit" formaction="/closeNotifLogin" class="text-sm font-VarelaRound text-gray-700 hover:text-main font-bold ml-3">&#x2715</button>
                </form>
            </div>
        </div>
        <% } %>
        
        
        <!-- Main Login Form -->
        <div class="flex items-center min-h-screen bg-gray-100 ">
            <div class="container mx-auto ">
                <div class="max-w-md mx-auto my-10 ">
                    <div class="text-center">
                        <h1 class="my-3 text-3xl font-semibold text-gray-700 ">Sign in</h1>
                        <p class="text-gray-500 ">Sign in to access your account</p>
                    </div>
                    <div class="m-7">
                        <form action="/server.js" method="post">
                            <div class="mb-6">
                                <label for="tp" class="block mb-2 text-sm text-gray-700 ">TP Number</label>
                                <input type="text" name="tp" id="tp" placeholder="TP123456" class="w-full px-3 py-2 placeholder-gray-400 border border-gray-300 rounded-md focus:outline-none focus:ring focus:ring-indigo-100 focus:border-indigo-300 " />
                            </div>
                            <div class="mb-6">
                                <div class="flex justify-between mb-2">
                                    <label for="password" class="text-sm text-gray-700 ">Password</label>
                                    <!-- <a href="#!" class="text-sm text-gray-500 focus:outline-none focus:text-indigo-500 hover:text-indigo-500 ">Forgot password?</a> -->
                                </div>
                                <input type="password" name="password" id="password" placeholder="Your Password" class="w-full px-3 py-2 placeholder-gray-400 border border-gray-300 rounded-md focus:outline-none focus:ring focus:ring-indigo-100 focus:border-indigo-300" />
                            </div>
                            <div class="mb-6">
                                <button type="submit" formaction="/login" class="w-full px-3 py-4 text-white bg-indigo-500 rounded-md hover:bg-indigo-600 hover:outline-none">Sign in</button>
                            </div>
                            <p class="text-sm text-center text-gray-500">Don&#x27;t have an account yet? <a href="/sign-up" class="text-indigo-400 focus:outline-none focus:underline focus:text-indigo-500 ">Sign up</a>.</p>
                        </form>
                    </div>
                </div>
            </div>
        </div>

        <!-- Notifications -->



    </body>
</html>